return {
    Status = {
        StatusMax      = 1000000,
        TickTime       = 1000,
        UpdateInterval = 30000,
        Display        = false -- Enable the esx_status bars (disable if you are using another HUD)
    }
}
